export const greet = (name: string): string => {
    return `Hello, ${name}! Welcome to the Algorand Username Service utilities.`;
  };
  
  // Another utility function placeholder
  export const getAUNSRecord = (username: string): string => {
    // Placeholder implementation - replace with actual logic to query AUNS
    return `Record for ${username}: [Placeholder data]`;
  };

  